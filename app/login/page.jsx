import ClientLogin from '@/components/ClientLogin';

export const metadata = {
  title: 'Client Portal Login — ARNE STORIES',
  description: 'Sign in to access your booking history, milestones, and transaction receipts.',
};

export default function LoginPage() {
  return <ClientLogin />;
}
